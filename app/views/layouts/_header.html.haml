%header
  %nav
    %ul
      - if current_user
        %li=link_to "Log Out", session_path, method: :delete
      - else
        %li=link_to "Sign Up", signup_path
        %li=link_to "Log In", login_path
  =link_to image_tag("logo.png", alt: "Happ"), root_path, class: 'logo'